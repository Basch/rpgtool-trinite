<ul class="nav {#flex-column#} bo-bookmarks w-100">
    {% for item in srv.user.menu %}
    <li class="nav-item">
        <div></div><a class="nav-link" href="{{ path( item.route ) }}" >{{ item.title }}<i class="fas fa-{{ item.icon }}"></i></a>
    </li>
    {% endfor %}
</ul>