<ul class="nav flex-column float-left">
    {% for item in side_menu.menu %}
    <li class="nav-item">
        <a class="nav-link" href="{{ path( item.route ) }}" >{{ item.title }}</a>
    </li>
    {% endfor %}
</ul>