{% extends 'base.html.twig' %}

{% block title %}Hello!{% endblock %}

{% block container %}

        <h1>Objet</h1>

        <p><a href="{{ path( item ~' .new' ) }}" class="btn btn-success" >Nouveau</a></p>

        Liste des atouts
        <ul>
        {% for item in items %}
            <li><a href="{{ path( class ~ '.show', { 'itemSlug': item.slug }) }}">{{ item }}</a></li>
        {% endfor %}
        </ul>

{% endblock %}