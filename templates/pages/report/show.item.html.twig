{% extends 'base.html.twig' %}

{% block title %}Hello!{% endblock %}

{% block container %}
    {% set character = srv.user.character %}

    {% if item.writer == srv.user.character %}
        <a href="{{  path( route ~ '.edit', { 'itemSlug': item.slug }) }}" class="btn btn-success" > éditer </a>
    {% endif %}

        <h1>{{ item }}</h1>

        <p>Titre : {{ item.title }}</p>
        <p>{{ item.text }}</p>

    <a href="{{  path( route ~ '.edit', { 'itemSlug': item.slug }) }}" class="btn btn-success" > commenter </a>
    {% for comment in srv.comment.fromItem( item ) %}
        <p>{{ comment.writer.name }}<br>{{ comment.text }}</p>
    {% endfor %}

{% endblock %}